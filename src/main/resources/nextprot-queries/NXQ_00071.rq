#id:NXQ_00071
#title:Proteins associated with at least 3 papers that are not "large scale" and not "additional"
#tags:unpublished,publications
#acs:
#count:
#time:

select distinct ?entry where {
  ?entry :isoform ?iso.
}
