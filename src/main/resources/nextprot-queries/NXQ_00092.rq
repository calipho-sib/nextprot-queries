#id:NXQ_00092
#title:Proteins with a sequence containing "FF.QYD" where "." is any single amino-acid
#tags:sequence
#acs:
#count: 0
#time: 0.6

select distinct ?entry where {
  ?entry rdf:type :Entry.
  ?entry :isoform / :sequence / :chain ?chain.
  filter ( regex(?chain, "FF.QYD") )
}
