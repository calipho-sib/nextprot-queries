#title:Proteins with a sequence containing "FF.+QYD" where ".+" is any peptide of any length
#tags:sequence
#acs:A7KAX9,A5YKK6,O60494
#count:187
#time:0.8

select distinct ?entry where {
  ?entry :isoform / :sequence / :chain ?chain .
  filter ( regex(?chain, "FF.+QYD") )
}
