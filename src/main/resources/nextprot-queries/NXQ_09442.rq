#id:NXQ_09442
#title:Check there are no entries annotated to SL-0191 » Nucleus which do not have the Swiss-Prot keyword KW-0539 » Nucleus
#tags:QC,keyword,subcellular location
#acs:NX_P57729,NX_Q2TAA8,NX_Q8TE56,...
#count:62
#time:0.803

select distinct ?entry ?subcell where {
  ?entry :isoform ?iso.
  ?iso :subcellularLocation /:term ?subcell.
  filter (?subcell in (cv:SL-0191))
  filter not exists {?entry :isoform /:keyword / :term cv:KW-0539.}
}
