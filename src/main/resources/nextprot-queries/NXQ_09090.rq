#id:NXQ_09090
#title:Check there are no entries with absorption maximum annotations with evidence from UniProt of quality SILVER
#comment:biophysicochemical property
#tags:QC,quality,evidence
#acs:
#count:0
#time:1.976

select distinct ?entry where {
  ?entry :isoform / :variant /:evidence ?ev1.
  ?ev1 :assignedBy source:Uniprot.
  filter not exists{?ev1 :quality :GOLD }
}
