#id:NXQ_09169
#title:Check there are no entries with GO cellular component annotations with evidence from GDB, the evidence code not IEP and quality SILVER
#tags:QC,quality,evidence,cellular component
#acs:
#count:0
#time:0.619

select distinct ?entry where {
  ?entry :isoform /:goCellularComponent /:evidence ?ev1.
  ?ev1 :assignedBy source:GDB.
  ?ev1 :quality :SILVER.
  filter not exists{?ev1 :evidenceCode cv:ECO_0000270 }
}
limit 10
