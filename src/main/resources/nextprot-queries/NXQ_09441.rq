#id:NXQ_09441
#title:Check there are no entries annotated to SL-0086 » Cytoplasm which do not have the Swiss-Prot keyword KW-0963 » Cytoplasm
#tags:QC,keyword,subcellular location
#acs:NX_Q96A47,NX_Q93070,NX_Q96BY9
#count:3
#time:0.74

select distinct ?entry ?subcell where {
  ?entry :isoform ?iso.
  ?iso :subcellularLocation /:term ?subcell.
  filter (?subcell in (cv:SL-0086))
  filter not exists {?entry :isoform /:keyword / :term cv:KW-0963.}
}
